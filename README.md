# TraceLens
A diagnostic and observability tool designed to inspect how sessions are executed across agents and language models. It provides detailed visibility into which models are invoked per session, how agents delegate tasks, and how the primary agent orchestrates model usage.
